extends layout

block content
  h1= title

  ul
    each author in author_list
      li
        a(href=author.url) #{author.name}
        |  (#{author.lifespan ? author.lifespan + ' years' : 'N/A'})

    else
      li There are no authors.

